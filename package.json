{
  "name": "instaautouz",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"cd server && npm run dev\" \"cd client && npm run dev:frontend\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev:frontend",
    "build": "cd client && npm run build",
    "install:all": "cd client && npm install && cd ../server && npm install",
    "db:push": "cd server && npm run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
